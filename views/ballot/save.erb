<!DOCTYPE html>
<%#include "objects/ballot.h" %>
<% tfetch(QVariantMap, ballot); %>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= controller()->name() + ": " + controller()->activeAction() %></title>
</head>
<body>
<p style="color: red"><%=$ error %></p>
<p style="color: green"><%=$ notice %></p>

<h1>Editing Ballot</h1>

<%== formTag(urla("save", ballot["id"]), Tf::Post) %>
  <p>
    <label>ID<br><input type="text" name="ballot[id]" value="<%= ballot["id"] %>" readonly="readonly" /></label>
  </p>
  <p>
    <label>Election ID<br><input type="text" name="ballot[electionId]" value="<%= ballot["electionId"] %>" /></label>
  </p>
  <p>
    <input type="submit" value="Save" />
  </p>
</form>

<%== linkTo("Show", urla("show", ballot["id"])) %> |
<%== linkTo("Back", urla("index")) %>
</body>
</html>
